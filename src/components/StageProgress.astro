---
interface Props {
  stages: string[];
  current?: string;
}

const { stages, current } = Astro.props;
const currentIndex = current ? stages.indexOf(current) : -1;
---

<ol class="stage-progress">
  {
    stages.map((stage, i) => {
      const state =
        currentIndex === -1
          ? 'plain'
          : i < currentIndex
            ? 'done'
            : i === currentIndex
              ? 'active'
              : 'upcoming';
      const label = `${String(i + 1).padStart(2, '0')} ${stage}`;
      return (
        <li class={`stage stage--${state}`}>
          <span class="stage__dot" aria-hidden="true" />
          <span class="stage__label">{label}</span>
        </li>
      );
    })
  }
</ol>

<style>
  .stage-progress {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    width: 100%;
  }

  .stage {
    flex: 1;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .stage:not(:first-child)::before {
    content: '';
    position: absolute;
    top: 0.3rem;
    right: 50%;
    width: 100%;
    height: 2px;
    background: var(--border);
    z-index: 0;
  }

  .stage__dot {
    width: 0.65rem;
    height: 0.65rem;
    border-radius: 50%;
    background: var(--background);
    border: 2px solid var(--border-strong);
    position: relative;
    z-index: 1;
  }

  .stage--done .stage__dot,
  .stage--plain .stage__dot {
    background: var(--foreground);
    border-color: var(--foreground);
  }

  .stage--active .stage__dot {
    background: var(--accent);
    border-color: var(--accent);
    width: 0.8rem;
    height: 0.8rem;
  }

  .stage__label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-top: 0.5rem;
    color: var(--muted-foreground);
  }

  .stage--done .stage__label,
  .stage--plain .stage__label {
    color: var(--foreground);
    opacity: 0.85;
  }

  .stage--active .stage__label {
    color: var(--accent);
    font-weight: 600;
  }

  @media (max-width: 640px) {
    .stage__label {
      font-size: 0.6rem;
    }
  }
</style>
